{
  "mcpServers": {
    "salesforce-dx": {
      "command": "npx",
      "args": [
        "-y",
        "@salesforce/mcp",
        "--orgs", "DEFAULT_TARGET_ORG",
        "--toolsets", "core,orgs,metadata,data,lwc-experts,testing,code-analysis"
      ]
    }
  }
}